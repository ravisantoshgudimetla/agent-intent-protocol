---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: safetypolicies.governance.aip.example.com
spec:
  group: governance.aip.example.com
  names:
    kind: SafetyPolicy
    listKind: SafetyPolicyList
    plural: safetypolicies
    shortNames:
    - sp
    singular: safetypolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.activeRules
      name: Rules
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SafetyPolicy is the Schema for the safetypolicies API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of SafetyPolicy
            properties:
              rules:
                description: Rules are the policy checks to evaluate
                items:
                  description: PolicyRule defines a single policy check
                  properties:
                    action:
                      description: Action determines what to do if the rule matches
                      enum:
                      - Deny
                      - Allow
                      - Log
                      - RequireApproval
                      type: string
                    config:
                      description: Config is the type-specific configuration (JSON)
                      x-kubernetes-preserve-unknown-fields: true
                      type: object
                    expression:
                      description: Policy expression in supported language (e.g. CEL) Evaluates to a boolean.
                      type: string
                    message:
                      description: Message is the human-readable explanation
                      type: string
                    name:
                      description: Name is the identifier for this rule
                      type: string
                    type:
                      description: Type is the kind of check
                      enum:
                      - StateEvaluation
                      - TimeWindow
                      - RateLimit
                      type: string
                    allowCompensating:
                      description: When true, this rule is bypassed for requests marked isCompensating=true (rollbacks). Has no effect on Deny rules — Deny is never bypassable.
                      type: boolean
                      default: false
                  required:
                  - action
                  - name
                  - type
                  type: object
                minItems: 1
                type: array
              cascadeDepth:
                description: Maximum transitive cascade evaluation hops (default 3). Set to 0 to disable cascade evaluation for this policy.
                type: integer
                format: int32
                minimum: 0
                default: 3
              targetSelector:
                description: TargetSelector determines which resources this policy applies to
                properties:
                  matchAttributes:
                    description: Key-value pairs that must match the target's attributes
                    type: object
                    additionalProperties:
                      type: string
                  matchResourceTypes:
                    description: Resource types this policy applies to
                    type: array
                    items:
                      type: string
                  matchActions:
                    description: Actions this policy applies to. Omit for all.
                    type: array
                    items:
                      type: string
                type: object
              failureMode:
                description: Behavior when policy evaluation dependencies are unreachable
                enum:
                - FailClosed
                - FailOpen
                default: FailClosed
                type: string
            required:
            - rules
            - targetSelector
            type: object
          status:
            description: status defines the observed state of SafetyPolicy
            properties:
              activeRules:
                description: ActiveRules is how many rules are currently active
                format: int32
                type: integer
              lastEvaluated:
                description: LastEvaluated is when this policy was last checked
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

